# Dependency directories
node_modules/
jspm_packages/
web_modules/

# Next.js build output
**/.next/
out/

# OpenNext build output (cdk-nextjs-standalone)
**/.open-next/

# CDK build output (cdk.out + per-deploy variants like cdk.out.deploy-dev)
**/cdk.out/
**/cdk.out.*/
cdk.context.json
*.snap

# TypeScript incremental build info
*.tsbuildinfo

# Build and distribution directories
dist/
build/
bin/
obj/
.serverless/
.esbuild/

# …but the CDK app entry lives in Source/infra/bin (TypeScript, not compiled output),
# so re-include it (the bin/ rule above is for build artifacts).
!Source/infra/bin/
!Source/infra/bin/**

# Local env files (secrets, config)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env.local
.env*.local
# Generated per-deploy by scripts/aws/deploy.sh
.env.production

# Debug and performance logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.log
logs/

# Diagnostic reports
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Keystore files
*.keystore
*.jks
*.p12
*.pfx
*.pem

# Test / Coverage outputs
coverage/
.nyc_output/
htmlreport/
playwright-report/
test-results/
.playwright-cache/

# OS-specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDEs and Editors (VS Code, IntelliJ, etc.)
.vscode/
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/settings.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Flutter/Dart (for future mobile app)
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/flutter_*.png
/flutter_*.jpg
/flutter_*.jpeg

# Local simulators/Docker
.localstack/


# Internal documentation
MCP_KB_DOCS.md

# Local files
local/**

storybook-static/

# Vision-cost benchmark corpus — real dev receipts pulled on-demand, never committed
invoices/fixtures/benchmark-corpus/