Brand logo — the double-loop crossover wave. Two indigo→teal curves cross to evoke "outsmarting inflation."

```jsx
<WobblioLogo size={38} />
<WobblioLogo size={28} withWordmark />
```

`withWordmark` appends the `wobbl`+`io` lockup (the `io` is brand indigo). The mark has no background box — place it directly on glass or the deep-space background. Never recolor the gradient.
