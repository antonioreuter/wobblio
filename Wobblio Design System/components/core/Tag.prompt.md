Metadata tag — neutral, square-cornered chip for invoice labels and item categories.

```jsx
<Tag>weekly</Tag>
<Tag removable onRemove={() => drop(id)}>dinner</Tag>
```

Quieter than `Badge` (no uppercase, no tone color). Use in table cells and tag rows. Set `removable` for editable tag composers.
