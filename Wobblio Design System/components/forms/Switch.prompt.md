Toggle switch — on/off setting; the "on" state is teal to signal a safe/active choice.

```jsx
<Switch defaultChecked />
<Switch checked={contributes} onChange={e => setContributes(e.target.checked)} />
```

Used for settings like "Contribute to Price Index". 46×24px track with a 18px thumb.
