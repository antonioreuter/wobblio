Text field — labeled input for forms and the parse-correction panel.

```jsx
<Input label="Merchant" defaultValue="Jumbo Oostpoort" />
<Input icon={<SearchIcon/>} placeholder="Search merchant, tag, or item…" />
<Input label="Item name" flagged defaultValue="Latte Macch" />
```

`flagged` paints the amber low-confidence treatment used when AI extraction confidence is below threshold. 44px tall to meet touch targets.
