Checkbox — multi-select control for shopping-list items and report retailer filters.

```jsx
<Checkbox defaultChecked label="Albert Heijn" />
<Checkbox label="Organic Whole Milk 1L" onChange={toggle} />
```

20px square, brand-filled when checked. Pass `label` to render the text affordance inline (whole row is clickable).
